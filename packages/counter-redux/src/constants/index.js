import * as counter from './counter';
import * as counterReusable from './counter-reusable';
import * as counterReusableArray from './counter-reusable-array';

export default {
    counter,
    counterReusable,
    counterReusableArray,
};